#include <stdlib.h>

#include <time.h>

#include <stdio.h>


/**
 * main - Print the alphabeics
 * Return: Always (Success)
 */
int main(void)
{
	char k;
	for (k = 'z'; k => 'a'; k--)
	putchar(k);
	putchar('\n');
	return (0);
}	
