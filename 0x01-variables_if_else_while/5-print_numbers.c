#include <stdlib.h>

#include <time.h>

#include <stdio.h>

/**
 * main - Entry poit 
 * Return: Always 0 (Success)
 */
int main(void)
	{
		int a;
		for (a = 0; a < 10; a++)
		{
		printf("%d \n", a);
		}
		return (0);
	}

