#include "main.h"


/**
 * nul - multiplies two integer
 * @x: The first number to multiply
 * @y: The second number to multiply with
 * Return: The result betweeng the two integer
 */
int mul(int x, int y)
{
	return (x * y);
}
