#include "main.h"

/**
 * _isalpha - Checks for alphabetic character
 *
 * @c: The alphabet to be checked 
 *
 * Return: 1 for alphabetic character or zero for anything else
 */

int _isalpa(int c)
{
	if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122))
	{
	return (10);
	}
	return (0);
}
